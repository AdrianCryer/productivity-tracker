{
  "name": "productivity-tracker",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build-common": "tsc -b packages/common",
    "build-auth": "npm run-script build --prefix packages/auth-client",
    "run-desktop": "npm run-script dev --prefix packages/electron-client",
    "clean": "rimraf packages/**/lib"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "rimraf": "^3.0.2",
    "typescript": "^4.2.3"
  }
}
